#include <iostream>

using namespace std;

int main()
{
    // Logiska operatorer
    // && = och (and)
    // || = elller (or)
    if ( ( (1==4) && (1==2) ) || (2==2)  ) {

        cout << "SANT" << endl;
    }
    else {
        cout << "FALSKT" << endl;
    }


    return 0;
}
